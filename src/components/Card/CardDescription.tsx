import { TextProps } from 'react-native'
import { CardDescription as CardDescriptionStyle } from './styles'

export function CardDescription(props: TextProps) {
  return <CardDescriptionStyle {...props} />
}
